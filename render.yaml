services:
  - type: web
    name: dataelec-frontend
    runtime: static
    buildCommand: npm ci && npx vite build
    staticPublishPath: ./dist
    autoDeployTrigger: commit
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
